# MVP 范围

PyCoach Lab 第一版仅验证迭代器课程的双 Agent 学习闭环。Questioner 负责正式出题，Critic 负责审题、理解学生意图、评价与讨论。后端只做编排、校验、持久化和失败回退。

课程范围：可迭代对象、迭代器、`iter()`、`next()`、迭代状态、耗尽和 `StopIteration`。

明确排除：代码执行、登录、支付、排行榜、社区、Redis、Celery、Neo4j、向量数据库、多 Agent 框架和第三个运行时 Agent。

